// TODO: use during build
// # fix --release-channel not available for web yet
// cp -f app/env/Environment.Prod.ts app/env/Environment.ts

export const Environment = {
  api: {
    baseUrl: "https://api.fiat2defi.ch/api/v1"
  }
}
